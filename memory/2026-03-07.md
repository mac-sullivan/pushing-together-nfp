# 2026-03-07 — Session Notes

## Project: Casa Madre
Restaurant website for a high-end Mexican restaurant in Tucson, AZ.
Located at: `/Users/macsullivan/Desktop/wren-creations/casa-madre/`
Stack: Next.js, Framer Motion, Sanity CMS, Tailwind, TypeScript

### Brand
- Dark moody palette: `--bg: #0d0905`, `--amber: #c8862a`, `--amber-light: #e6a84a`, `--cream: #d8dce0`
- Fonts: Cormorant Garamond (serif/headings), DM Sans (body), Cinzel (buttons)
- Existing botanical watermarks (agave SVG, nopal cactus SVG) in homepage sections

### Work done today

**Nav (Nav.tsx):**
- Nav link hover color changed from `var(--muted)` → `var(--amber)` (orange, not white)
- Nav link default color changed from `var(--muted)` → `rgba(216,220,224,0.82)` for readability over hero image
- Added `textShadow: '0 1px 8px rgba(13,9,5,0.75)'` to nav links and phone number
- Phone number default color: `rgba(216,220,224,0.55)`
- Logo font size bumped from `1.1rem` → `1.45rem`
- Active nav underline: removed `layoutId="nav-active-bar"` (was animating bottom-to-top on page change), replaced with `scaleX: 0 → 1` wipe-in from left

**Phone number consistency:**
- Standardized to `(520) 555-0123` / `tel:+15205550123` across all files
- Was previously `(520) 000-0000` in most places, `555-0123` only in nav

**About page (AboutClient.tsx):**
- Removed `overflowX: 'hidden'` from root div — was breaking Framer Motion `whileInView` (IntersectionObserver issue). Used `overflow: clip` on hero section instead.
- Story image: scroll-triggered fade+slide in (`initial: opacity 0, x: -32`)
- Chef image: scroll-triggered fade+scale in
- Both images: removed all decoration experiments (agave SVG, palm SVG, corner brackets, shimmer sweep, floating rectangles) — user didn't like any of them
- All sections use `whileInView` + `once: true` for cascade-on-scroll animations

**HomeClient.tsx:**
- Removed `overflowX: 'hidden'` from root div (same fix)
- Location/hours image: clean scroll fade-in, no decorations
- `PalmDecoration` import removed

**Contact page (contact/page.tsx):**
- Map section rebuilt: height increased to `clamp(500px, 60vh, 640px)`
- Map filter: `grayscale(1) brightness(0.2) sepia(0.55) contrast(1.1)` — dark atmospheric
- Floating info card overlaid on map (frosted glass, amber border, address/phone/email/directions button)
- Removed `overflowX: 'hidden'` from root div
- Card animates in from left with `whileInView`

**PalmDecoration component created** (`/components/PalmDecoration.tsx`) — not currently used but exists with variant A (canopy) and variant B (arching frond)

### Key lessons
- `overflowX: hidden` on a root wrapper div breaks Framer Motion `whileInView` in some browsers — use `overflow: clip` on specific sections instead
- Framer Motion `layoutId` across page transitions animates vertically (bad) — use explicit `scaleX` animations instead
- Mac is particular about decorative elements — went through: floating rectangles → offset border rect → corner brackets → agave SVG → palm SVG → removed all. Keep it clean.
- Mac's style preference: clean, luxurious, minimal decoration. Scroll animations yes, busy graphics no.

### User preferences observed
- Likes: subtle scroll-triggered animations, clean layouts, amber/gold accents
- Dislikes: decorative SVG graphics behind images (tried many, all rejected)
- Dislikes: shimmer sweep animation on images
- Prefers orange (#c8862a) hover states over white/cream

---

## Session 2 (afternoon) — Pushing Together NFP + Casa Madre continued

### Pushing Together NFP — Work done

**Hero section:**
- Hero image inset 1.5rem on all sides (creates floating card look with dark bg showing around edges)
- `border-radius: 16px` on hero image and overlay
- Hero content: left-aligned, bottom of hero, `align-items: flex-end`
- Previous attempt at centered hero text — reverted at user request

**Header/Nav:**
- `#site-header` changed from `left:0; right:0; padding: 0 2rem` → `left:50%; transform:translateX(-50%); width:calc(100% - 3rem); max-width:1200px`
- This centers the header with equal gutter, logo left / nav pill right balanced at same max-width as content
- Logo max-width: 576px (native SVG viewBox width)
- Double anchor bug fixed: `the_custom_logo()` outputs its own `<a>` — changed outer wrapper from `<a class="nav-logo">` to `<div class="nav-logo">` in both header.php and footer.php

**SCSS migration (major work):**
- Converted entire theme from flat `style.css` to modular SCSS
- Installed Dart Sass globally via npm (`sass@1.97.3`)
- Created full SCSS structure at `assets/scss/`:
  - `abstracts/_variables.scss` — brand colors, spacing, $max-width, $font-main, CSS custom props
  - `abstracts/_mixins.scss` — @mixin container, section-padding, mobile/tablet/desktop, flex-center, absolute-fill, accent-heading
  - `abstracts/_index.scss` — forwards variables + mixins
  - `base/_reset.scss`, `_typography.scss`, `_utilities.scss`
  - `components/_buttons.scss`, `_nav.scss`, `_footer.scss`
  - `layouts/_hero.scss`, `_mission.scss`, `_sponsors.scss`, `_full-photo.scss`, `_text-block.scss`, `_two-col.scss`, `_stats.scss`, `_newsletter.scss`, `_photo-grid.scss`, `_cta-banner.scss`, `_events.scss`, `_video.scss`
  - `main.scss` — entry point, imports all with @use
- Each partial uses `@use '../abstracts' as *` for variables/mixins
- `_mixins.scss` uses `@use 'variables' as *` internally
- Compiled to `assets/css/main.css` (compressed, no source maps for prod)
- `package.json` scripts: `sass:build` (compressed) and `sass:watch` (expanded + source maps)
- `functions.php` updated: enqueues `assets/css/main.css` with `filemtime()` cache busting instead of `style.css`

**Key file paths:**
- Theme: `/Users/macsullivan/Local Sites/pushing-together-nfp/app/public/wp-content/themes/blankslate-child/`
- SCSS entry: `assets/scss/main.scss`
- Compiled CSS: `assets/css/main.css`
- Run `npm run sass:watch` from theme root to auto-compile

### Mac's preferences (observed this session)
- Wants modular, organized code (SCSS per component vs one big file)
- Reverted hero text centering immediately — prefers left-aligned hero with text at bottom
- Cares about clean HTML (caught double anchor tag right away)
- Uses VS Code as primary editor; likes having it opened directly

---

## Session 3 (late afternoon) — Pushing Together NFP continued

### Awwwards-style redesign work

**Fonts:**
- Switched heading font from Syne → **Plus Jakarta Sans** (cleaner, less heavy)
- Both fonts loaded in header.php Google Fonts link + typography.scss @import
- Body font remains Inter

**Hero:**
- Hero image now full-bleed (fills entire dark card) with gradient overlay (bottom-to-top dark)
- Text overlaid at bottom-left of card
- `mix-blend-mode: exclusion` tried on logo then removed — caused readability issues
- Hero card has grain texture via SVG data URI + `::after`
- Purple radial glow behind content
- Scroll indicator (animated drip line + "Scroll" label) at bottom center
- Hero text stagger animations: eyebrow→h1→p→buttons each delay 0.55/0.7/0.85/1.0s

**Navigation:**
- Logo and nav-right pill: both `height: 51px`, `border-radius: $radius-lg (24px)`
- Logo color: white on dark hero, purple on light backgrounds
- Logo color state managed via JS class `over-dark-hero` on `#site-header` (not `.home` body class)
- `over-dark-hero` class added whenever page has `.section-hero` OR `.page-hero`
- Removed all hover transforms (no jumping on logo or donate button)
- Logo hover: purple glow via `filter: drop-shadow` only
- Removed magnetic button effect from JS entirely

**New components added:**
- `_cursor.scss` — custom cursor (hidden, not active — Mac removed it)
- `_marquee.scss` + `template-parts/marquee.php` — purple text strip after hero ("Pushing Together · Skate. Grow. Thrive.")
- `_loader.scss` — page loader (Mac removed it — didn't want loading animation on refresh)
- `_page-hero.scss` + `template-parts/page-hero.php` — interior page hero (dark card with page title, breadcrumb, grain, purple glow)
- Interior page hero wired into `page.php` before ACF flexible content sections

**Sponsors section:**
- Removed marquee carousel
- Now static grid with `grid-template-columns: repeat(auto-fit, minmax(180px, 1fr))`
- Chips separated by 1px lines (grid gap trick with background on parent)
- Heading changed to "Thank you to our awesome sponsors"
- Logos grayscale → full color on hover

**Photo grid / Journey carousel:**
- Fixed Swiper config: removed `1.15`/`2.15`/`3.15` fractional slidesPerView (was peeking at other slides)
- Now: 1 slide mobile, 2 on 600px+, 3 on 1024px+, `overflow: hidden`
- Loop: true

**Grain overlay:**
- Page-wide grain via `body::after` — animated 9s steps(10) infinite
- Also applied inside hero/page-hero cards

**Spacing:**
- Hero section top padding: `51px` (exact nav height, no extra gap)
- All sections use `1.5rem` as base left/right margin (matches nav pill position)
- Container padding changed from `2rem` → `1.5rem` in `_mixins.scss`

**JS (`main.js`):**
- Removed page loader IIFE
- Removed magnetic button effect
- Header scroll threshold: based on hero height (`hero.offsetHeight - 100`) not fixed 60px
- `over-dark-hero` class toggled on scroll based on hero height threshold

### Key lessons from this session
- Mac quickly rejects animations that feel "busy" — removed loader, removed magnetic buttons, removed custom cursor
- Mac edits SCSS directly himself sometimes — check file before overwriting
- Nav `top` was changed by Mac from `1.25rem` to `.25rem` mid-session — hero padding must track this
- Alignment is important to Mac — spent significant time aligning hero text with logo position
- Mac runs `npm run sass:watch` himself in terminal during active dev

## Session continuation — afternoon (Pushing Together NFP hero + logo)

### Hero redesign — Daylight Computer inspiration
- Visited https://daylightcomputer.com/ to analyze hero layout
- Key design DNA extracted: hero as dark card (not full bleed), nav INSIDE the dark card, two-column split (text left / image right as design element, not background), compact ~88vh height, soft off-white page bg shows around card, bottom corners only rounded
- Implemented in `_hero.scss` + `hero.php`: `.section-hero` has `padding: 0 1.5rem 1.5rem` (no top padding so card butts against page top), `.section-hero-inner` has `border-radius: 0 0 20px 20px`
- `.hero-nav-row` lives inside `.section-hero-inner` — contains logo + nav pill + donate button
- `.hero-body` uses `display: grid; grid-template-columns: 1fr 1fr` — text left, image right
- Hero image in right column uses `object-fit: cover` inside `.hero-image-wrap`, with gradient fade on left edge (`::before`) blending into dark bg
- `.home #site-header { display: none }` hides fixed nav on homepage since hero has its own

### Logo update — inline SVG
- User sent actual brand SVG: icon mark (skateboard/wheel motif) + "Pushing Together" wordmark text as SVG paths
- ViewBox `0 0 205 32`, all paths use `fill="var(--logo-color, #fff)"` — controllable via CSS custom property
- Saved as `assets/images/pt-logo.svg` (not using WP custom_logo img tag anymore)
- Added `pt_logo_svg($height, $class)` helper to `functions.php` — uses `file_get_contents()` for inline output
- Used in: `hero.php` (hero nav), `header.php` (fixed nav), `footer.php`
- CSS variable color control:
  - Hero nav: `--logo-color: #fff` (always white on dark card)
  - Fixed pill nav (`#site-header`): `--logo-color: #080808` (dark on light pill)
  - Footer: `--logo-color: rgba(255,255,255,0.85)` (white on dark footer)
- `mix-blend-mode: unset` applied everywhere — user specifically requested this to prevent blend compositing artifacts

### Key Mac preferences observed
- Inline SVG preferred for logos — gives full CSS color control without img/filter hacks
- `mix-blend-mode: unset` is a hard requirement for logos
- Likes editorial, restrained hero layouts (not loud full-screen takeovers)
- Daylight Computer site is a strong design reference for him

---

## Session 4 (evening) — Pushing Together NFP nav + blog

### Nav underline — sliding indicator across pages
- Removed glowing dot (`::after` pseudo-element) from active nav links
- Replaced with `.nav-underline` — a JS-injected `<span>` absolutely positioned inside `.nav-links` (`bottom: 3px`, `height: 2px`, `background: $purple`)
- Uses `sessionStorage` to persist previous active link index across page loads, so underline slides from old position to new on navigate
- **No hover effect** — Mac explicitly rejected hover sliding; underline only tracks active page
- JS stores `pt_nav_from` in sessionStorage on click, reads + removes on load, animates from prev to current

### White flash fix
- `html { background: #0d0d0d }` prevents white initial paint
- Removed `body { animation: pt-page-in }` CSS animation (was causing issues)
- Nav logo + right pill both start as **dark glass** (`rgba($white, 0.10)` + backdrop-filter) — not white — so no flash on dark hero pages
- Transition to light glass on `.scrolled` state

### Page transitions — removed
- Mac didn't want any page transition overlay (was causing black screen)
- All overlay JS removed; pages load instantly with no animation

### Mobile nav — one rectangle
- Mobile header is now a single glassmorphism rounded rectangle (`border-radius: 20px`)
- Individual pills removed from logo and nav-right on mobile
- Both `background: none`, `border: none`, `box-shadow: none` on mobile
- `#site-header` itself carries the glass effect on mobile
- Mobile dropdown menu: dark glass background (`rgba(#0a0a0a, 0.95)`) not white

### Nav link color states
- On dark hero (before scroll): links are `rgba($white, 0.8)`, active = `$white`, underline = `$white`
- On scrolled (light pill): links are `$gray-800`, active = `$purple`, underline = `$purple`

### Blog — built from scratch
- Set `page_for_posts` to 7 (Blog page) — was `0` before
- Created `home.php` — blog archive template with dark header + 3-column card grid
- Created `single.php` — individual post template with hero (SVG image full-bleed, dark overlay), body typography, prev/next nav cards
- Created `assets/scss/layouts/_blog.scss`, added `@use 'layouts/blog'` to main.scss
- Created 3 real blog posts (IDs 178, 179, 180) with proper content + categories:
  - "Skateboarding Changed My Life — A Story From DeKalb" (Stories) — image ID 175
  - "What We Learned Running Free Skate Clinics This Summer" (Programs) — image ID 176
  - "How to Donate Skate Gear (And Why It Matters)" (Get Involved) — image ID 177
- Placeholder images: SVG gradients (purple/dark) uploaded as blog-placeholder-1/2/3.svg (IDs 175–177)
- NOTE: 2 leftover default WP posts ("Hello world!" and "Pushing together blog post") still in DB — Mac should trash from WP Admin

### File tracking
- New templates: `home.php`, `single.php`
- New SCSS: `assets/scss/layouts/_blog.scss`
- Modified: `assets/scss/components/_nav.scss` (major rewrite — dark glass default, mobile unified bar)
- Modified: `assets/scss/base/_reset.scss` (removed page-in animation, added html dark bg)
- Modified: `assets/js/main.js` (removed overlay, added sessionStorage underline logic)
