# Memory — Sunday, March 8, 2026

## Pushing Together NFP — Inner Hero Refactor

### What was built
- Created `assets/scss/layouts/_inner-hero.scss` — shared `.pt-hero` base class with keyframes (`pt-hero-card-in`, `pt-hero-img-settle`, `pt-hero-fade`, `pt-hero-text-up`) and sub-elements (`.pt-hero-bg`, `.pt-hero-overlay`, `.pt-hero-glow`, `.pt-hero-content`, `.pt-hero-eyebrow`, `.pt-hero-heading`, `.pt-hero-sub`, `.pt-hero-badges`, `.pt-hero-badge`, `.pt-hero-scroll`)
- Rewrote `template-parts/page-hero.php` to emit `<section class="pt-hero page-hero-inner">` with per-slug image map (contact=82, about=79, blog=80, donate=11) and per-slug eyebrow/subtitle defaults
- All four interior pages now use `.pt-hero` as base class: About (`class="pt-hero about-hero"`), Donate (`class="pt-hero donate-hero"`), Contact (via page-hero.php), Blog archive (via home.php)
- Added `@use 'layouts/inner-hero';` to `main.scss` (before `@use 'layouts/about'`)
- Removed duplicate keyframes from `_about.scss` and `_donate-page.scss`

### Contact page fix
- Found a hidden `page-contact.php` template (WordPress was using it instead of `page.php`)
- It had no hero, no `<main>` wrapper
- Rewrote it to call `page-hero.php`, `contact_form`, and `cta_banner`

### SCSS cleanup (this session)
- `_blog.scss` — stripped old `.blog-archive-header` block (had margin, bg, etc.); replaced with slim override for `.pt-hero-content` padding
- `_page-hero.scss` — rewrote from scratch (orphaned keyframe `to{}` lines left after regex); now just contains `.page-hero` margin/radius wrapper
- `_about.scss` — stripped orphaned `to{}` keyframe fragments at top of file
- SCSS compiles cleanly as of this session

### Visual QA result
- Contact page: cinematic hero with photo (ID 82), "GET IN TOUCH" eyebrow, "Contact" h1, subtitle ✅
- Blog page: community photo (ID 79), "BLOG" eyebrow, "Stories, updates, and community." h1 ✅
- CTA banner text rendering on blog page ✅

### Pending / unresolved
- User said "undo those changes" at 02:29 AM — unclear which changes (this session's cleanup vs. the full inner-hero refactor). Awaiting clarification.
- Old page-transition JS block in `main.js` (lines ~174–228) still may need cleanup — `DOMContentLoaded` that sets `body.style.opacity = '0'` + conflicting `pageshow` handler
- Nav underline JS still needs injection (`<span class="nav-underline">` into `.nav-links`)
- Donate page visual QA still pending
- `[give_form id="162"]` rendering check still pending
- Casa Madre: leftover `<motion.div style={{ display: 'none' }}>` block in `AboutClient.tsx` still needs removal

## Notes
- Mac upgraded Claude plan (extra usage at $20.84 / $20 limit, reset Apr 1) — sessions were cutting off mid-work due to token limits; plan upgrade enables longer runs
- Browser tool opens in narrow/mobile viewport by default; must use `browser open` with `width: 1440` to get desktop view for visual QA
