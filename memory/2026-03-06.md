# Memory — 2026-03-06

## First Session with Mac

Today was my first day online. Mac Sullivan set me up and we had a long, productive first session.

### About Mac
- **Full name:** Mac Sullivan
- **Age:** 30
- **Location:** Tucson, AZ (America/Phoenix, no DST)
- **Job:** Frontend developer at Straight North
- **Background:** Worked at many companies/agencies
- **Freelance:** Active
- **Starting an agency:** Called **Stoop** — web design, SEO, ads, lead gen
- **Skateboard clients:** Has a couple already
- **Photography/video:** Hobby, has a real eye for it (dark, moody, atmospheric desert/night work)
- **Interests:** Mud flood theory, reset civilization, Tartaria, hidden history/alternative history
- **Personal site:** macsullivan.com (a few years old, dark aesthetic, Celtic knot logo, custom HTML/SCSS/JS)
- **Contact:** Telegram only (mac's phone → Mac mini via OpenClaw)

### My Identity
- **Name:** Wren (Mac named me during terminal setup)
- **Emoji:** 🪶
- **Nature:** AI assistant living on Mac's Mac mini in Tucson
- **Files:** IDENTITY.md, USER.md updated. BOOTSTRAP.md deleted.

### What We Built Today
1. **`~/Desktop/wren-creations/Wren/`** — My personal website (dark theme, about me)
2. **`~/Desktop/wren-creations/sacred-geometry/`** — Animated sacred geometry canvas art cycling through 6 modes (Mayan Fire, Cosmic Void, Electric Storm, Earth Temple, Crystal Ice, Blood Moon) with smooth morphing transitions
3. **`~/Desktop/wren-creations/casa-madre/`** — Full Next.js 14 + Tailwind + Framer Motion demo site for Casa Madre restaurant (Tucson pitch client)
4. **`~/Desktop/wren-creations/tucson-leads.md`** — Research doc: 10 Tucson businesses with no/weak web presence for Stoop outreach

### Casa Madre Site (Active Project)
- **Stack:** Next.js 14 app router, TypeScript, Tailwind, Framer Motion, Sanity CMS (schemas built, not yet connected)
- **Pages:** Home, Menu, About, Reservations, Contact
- **Running:** `npm run dev` at localhost:3000
- **Design:** Dark (#0d0905), amber/gold accents, Cormorant Garamond serif, custom CSS vars, 1220px container
- **Images:** Unsplash (restaurant interior, food photos) — next.config.ts allows unsplash domain
- **Sanity:** Schemas built (menuItem, siteSettings). Mac is currently running `npm create sanity@latest` to get a project ID. Once he pastes it, wire it into `.env.local` and connect the data fetching.
- **TODO:** Get Sanity project ID from Mac, wire up live CMS data, replace hardcoded menu/settings

### Tech Opinions Discussed
- Mac prefers writing his own CSS/SCSS over Webflow or Tailwind
- Recommended stack for Stoop: **custom SCSS + Astro (or Next.js) + Sanity**
- Webflow not right for Mac (fights the class system, doesn't need it)
- WordPress: avoid
- Sanity free tier is plenty for small agency clients
- Vercel free tier handles hosting; recurring $50-100/mo "maintenance" retainer is good revenue

### Capabilities Confirmed Working
- exec (shell commands, file creation)
- browser (open URLs, navigate, screenshot, click)
- read/write files
- Claude Code spawning (needs PTY + confirmed auth — Mac completed auth tonight)

### Notes
- Mac reaches me exclusively via Telegram on his phone
- No email/calendar integration yet
- No paired nodes
- Dev server running in background: `pkill -f "next dev"` to stop if needed

---

## Late-Night Session (1:00–1:15 AM)

### Casa Madre — Major Progress

**CMS fully seeded via script (no manual entry needed):**
- 16 menu items created programmatically using `@sanity/client` + auth token from `~/.config/sanity/config.json`
- All 16 images downloaded from Unsplash and uploaded to Sanity assets API, patched onto each item
- 8 FAQ items seeded
- About page document seeded
- Sanity project ID: `dn2vc01d`, dataset: `production`

**New Sanity schemas added:**
- `faqItem` — question, answer, order
- `aboutPage` — heading, subheading, story blocks, chef name/title/bio/image, gallery
- `menuItem` updated — heart icon toggle: featured items show `❤️ Name` in studio list; `featured: true` items appear on homepage
- `siteSettings` updated — added contactMapEmbed, privateEventsEmail

**New pages built:**
- `/faq` — server component + FaqClient with animated accordion
- `/about` — server component + AboutClient with story, chef bio, gallery grid

**Nav — complete redesign:**
- Centered CASA/MADRE wordmark with ✦ ornament and amber hairlines
- Stacked CASA (cream) / divider line / MADRE (italic amber)
- Desktop: logo left, links + Reserve button right
- Mobile: hamburger only (3 bars, animated to X), full-screen clip-path wipe overlay with large wordmark and staggered link reveal
- Responsive fix: `hidden md:flex` on desktop links, `flex md:hidden` on hamburger — inline style conflict was the bug that made it invisible

**FAQ redesign (Awwwards-level):**
- Giant `Preguntas` watermark at ~4% amber opacity behind hero
- "Everything / *you need* / to know." — fragmented editorial headline
- Accordion with `01 02 03` numbering, spring-eased expand, amber plus rotates to X
- CTA strip at bottom

**Contact redesign (Awwwards-level):**
- `Contacto` watermark in hero
- "Come find *us.*" — minimal power headline
- Two-col layout: address/phone/hours table/private events left; contact form right
- Form fields with floating amber underline focus states
- Google Maps iframe at bottom with grayscale/sepia filter + gradient vignette overlay

**Key lesson learned:**
- Never mix `className="hidden md:flex"` (Tailwind) with `style={{ display: 'flex' }}` (inline) on the same element — inline style wins and breaks responsive visibility
- Use `display` only via Tailwind OR only via inline, never both on the same element

**Dev server:** `npm run dev` at `localhost:3000` / `192.168.1.204:3000`
**Studio:** `localhost:3000/studio` → Structure to manage all content types

---

## Continued Session (1:45–2:09 AM)

### Casa Madre — Polish Pass

**Nav/header improvements:**
- Hero text left-aligned to same container as rest of page (wrapped in `maxWidth: 1220px / margin: 0 auto / padding: 0 2rem`)
- Desktop nav links right-aligned via `margin-left: auto` on `.nav-desktop-links`
- Nav container bumped to `maxWidth: 1358px` (35px wider each side vs 1220px page content) — Mac picked this after seeing his viewport size in DevTools
- Nav link color changed to `#e6a84a` (amber-light) — same as button color, not cream
- "Tucson, Arizona · Est. 2021" label: bumped to `0.65rem`, opacity `0.9`, added `text-shadow: 0 1px 8px rgba(0,0,0,0.8)` for legibility

**Gradient-to-solid nav transition fixed:**
- Old approach (binary `scrolled` state with CSS background swap) snapped harshly — CSS can't animate between gradient and solid
- New approach: gradient always on nav base, `motion.div` with solid bg fades opacity `0→1` via `useScroll` + `useTransform` over first 120px of scroll
- Link brightness wired to scroll position via `useMotionValueEvent`

**Page loader added (`components/PageLoader.tsx`):**
- Full-screen dark overlay matching `#0d0905`
- CASA/MADRE wordmark fades in centered at 600ms
- Amber progress line sweeps left-to-right below wordmark
- At 1.6s: clip-path wipe upward (curtain effect, sharp bezier curve) reveals the page
- Hero content delays shifted: location label at 1.7s, h1 at 1.85s, tagline+buttons at 2.2s
- Nav gets subtle entrance motion with 1.6s delay

**Footer rebuilt (`components/Footer.tsx`):**
- Full editorial 4-column grid: Brand/tagline · Navigate · Visit · Hours
- Giant ghost "Casa Madre" italic watermark at ~4% amber opacity
- Animated amber hairline top border (wipes left-to-right on scroll-in)
- CASA/MADRE wordmark repeated in brand column
- Hours table with cream text, "Closed" in muted italic
- Bottom bar: copyright left, Instagram/Facebook links center (hover → amber), coordinates right
- `'use client'` for Framer Motion animated border

**Menu page — sticky category nav with smooth scroll:**
- Category bar sticks at `top: 72px` (below fixed header)
- `IntersectionObserver` tracks active section as you scroll, highlights category in `#e6a84a`
- Click → JavaScript smooth scroll with precise offset (`NAV_H 72px + BAR_H 45px + 24px padding`)
- Framer Motion `layoutId` slides amber underline between tabs
- Bar darkens (`rgba(13,9,5,0.97)`) when pinned vs lighter when still in hero zone
- Converted `<a href="#...">` anchor links to `<button onClick={scrollToSection}>` to prevent jump

**About page — images seeded:**
- 7 Unsplash photos downloaded and uploaded to Sanity assets API
- Chef portrait: `photo-1577219491135` (warm kitchen lighting)
- 6 gallery images: dining room, kitchen, bar, patio, food detail (ceviche), food detail (short rib)
- aboutPage document patched with `chefImage` and `gallery` array referencing Sanity asset IDs
- AboutClient already had `urlFor` wired up — images appeared without any code change

**LangToggle component finalized:**
- Single `LangToggle` component (props: `lang`, `toggle`, optional `desktop?: boolean`)
- Desktop: `EN | ES` with divider, active language amber-light, inactive muted
- Mobile: shows only the opposite language label as compact pill
- Placed in `nav-hamburger` wrapper for mobile (hides on desktop via CSS)

---

## Evening Session (6:00–8:35 PM)

### Casa Madre — Major UI Polish Pass

#### Light/Dark Mode (added then removed)
- Built full `ThemeContext` with localStorage persistence + system preference detection
- CSS vars for `[data-theme="light"]` with warm linen tones (#f5ede0 bg, dark espresso text)
- `ThemeToggle` button in nav (sun/moon icons)
- **Removed at Mac's request** (~8:30 PM) — decided to stay dark mode only
- Cleaned up: ThemeProvider removed from layout, all light-mode CSS vars deleted, hero text vars hardcoded back to dark values

#### Hero Section Redesign
- Made hero full viewport height (100svh), full bleed edge-to-edge, sits behind nav
- Removed border around hero image
- Nav height bumped to 106px with 18px top + 18px bottom padding for breathing room
- Hero gradient overlay using CSS var (was needed for theme switching, now hardcoded dark)
- Location label ("TUCSON, ARIZONA · EST. 2021") — fixed readability with frosted dark pill: `background: rgba(0,0,0,0.38)`, `backdropFilter: blur(8px)`, always cream text regardless of photo
- Hero text: "Casa" always `#f5ede0`, "Madre" always amber — hardcoded since always over dark overlay
- Tagline upgraded: Cormorant Garamond italic, `clamp(1.25rem, 2.8vw, 1.75rem)`

#### Navigation
- **Font**: Open Sans for nav links (was Cormorant Garamond, then Playfair Display, settled on Open Sans)
- **Layout**: Logo left, all links + actions right (tried centered 3-column grid, reverted to simple flex)
- **Padding**: 18px top + 18px bottom, total height 106px
- Nav content hero text left-aligned to same 1220px container as page content below

#### Font System
- **Sans-serif**: Changed from Inter → DM Sans (elegant, readable at small sizes, optical sizing)
- **Body/description text**: Cormorant Garamond italic throughout (about, FAQ, contact, reservations, footer blurb, hero tagline, section descriptions)
- **Menu card descriptions**: Kept DM Sans (short, scannable context)
- **Labels/overlines**: DM Sans uppercase (unchanged)
- **Nav links**: Open Sans
- Google Fonts import updated to load all three families

#### Menu Page
- Desktop layout changed from list rows → card grid matching homepage (image on top, 16:9 aspect ratio, price badge on image, description below)
- Category nav bar + mobile tab bar font → Cormorant Garamond 1.15rem
- Menu page description text (intro): Cormorant Garamond italic

#### Decorative Elements
- Removed ghost numbers "01" and "02" from homepage sections
- Added **agave rosette SVG** (9 radiating leaves from center) — top-right of Featured Dishes section
- Added **nopal cactus SVG** (7 paddles, prickly pear fruits, spine dots, ground line) — bottom-left of Hours/Location section
- Removed ghost numbers from menu page sections too
- All botanicals: stroke only, amber color, ~5.5% opacity, scale with viewport

#### Layout Alignment
- Two-column header sections: `alignItems: 'baseline'` applied sitewide (home, about, contact, reservations, menu)
- Featured Dishes section: label pulled out of grid, only h2 + description in grid with `alignItems: 'start'`
- Hours & Location grid: reverted to `alignItems: 'start'` (baseline caused content to disappear with overflow:hidden children)

#### Animation Fixes
- PageLoader: reduced from 1600ms → 900ms timeout, progress bar 1.1s → 0.65s
- Hero animation delays cut roughly in half across the board
- `viewport={{ margin: '-80px' }}` added to all `whileInView` observers sitewide — prevents elements from staying at initial state when they enter viewport near the fold
- **Key lesson**: `overflow: clip` breaks `whileInView` IntersectionObserver detection — never use it on animation wrapper divs. Stick with `overflow: hidden`.
- **Key lesson**: `alignItems: baseline` + `overflow: hidden` on grid children can cause entire columns to become invisible — use `alignItems: start` for content sections with animated children

#### Bug Fixes
- Theme flash on load: resolved by removing theme system entirely
- Content hidden in Hours section: caused by `overflow: clip` breaking whileInView + `alignItems: baseline` conflict — both reverted

---

## Late Evening Session (~9:45 PM – 10:40 PM)

### Casa Madre — i18n + Mobile Polish

#### Full EN/ES Language Toggle (sitewide)
- Expanded `context/LanguageContext.tsx` to a comprehensive bilingual `t` object
- All static UI text now translated: nav, hero, strip, home, menu, about, faq, contact, reservations, footer
- Hours rows translated in both `t.hours.rows` (full days) and `t.hours.homeSummary` (abbreviated)
- All pages updated to consume `useLang()` + `t[section][key][lang]` pattern
- Pages updated: HomeClient, MenuClient, AboutClient, FaqClient, contact/page, reservations/page, Footer
- TypeScript: zero errors after all changes

#### About Page — Chef Section Layout Fix
- "EXECUTIVE CHEF & CO-FOUNDER" label + "Chef Elena Reyes" heading moved from above the grid to **top of the right column**
- Chef name font-size reduced from `clamp(2.5rem, 7vw, 6rem)` → `clamp(2.5rem, 5vw, 4.5rem)` to fit alongside image
- Layout now reads: left col = image | right col = label → name → bio → stats

#### Footer Cleanup
- Removed Instagram + Facebook text links from bottom footer bar
- Animated amber top border hidden on mobile (`className="hidden md:block"`) — was appearing as unwanted second orange line

#### Nav Link Style — Final
- Switched from Josefin Sans → **Cormorant Garamond** for desktop nav links
- `1rem`, `0.18em` letter-spacing, uppercase, `font-weight: 400`
- Active page: `var(--amber)` + animated amber underline (`layoutId="nav-active-bar"`)
- Inactive: `var(--muted)`, hover lifts to `var(--cream)`

#### Mobile Hamburger
- Bars: `var(--cream)` (silvery #d8dce0 — not white, not orange)

#### Mobile Button/Font Improvements
- Global `@media (max-width: 767px)` added to globals.css:
  - Buttons: `font-size: calc(0.78rem + 2px)`, `padding: 0.9rem 0.9rem`, `letter-spacing: 0.14em`, `text-align: center; justify-content: center`
  - Body: `font-size: calc(1rem + 2px)`
- Button spans: `white-space: nowrap` to prevent text wrapping to second line
- Hero button row: `flexWrap` removed so buttons always sit side by side
- Hero button gap: `0.75rem → 1.5rem`

#### Mobile Menu Cards
- Width: `72vw → 88vw` (nearly full screen width on mobile)
